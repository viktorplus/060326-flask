# Файл-маркер Python-пакета.
# Проект запускается изнутри своего каталога (cd l03_rest_api && python app.py),
# а модули импортируют друг друга абсолютно (from schemas import ...), поэтому
# для запуска этот файл не нужен — он оставлен только чтобы каталог оставался
# полноценным пакетом и его можно было при желании импортировать целиком.
# Файл намеренно пустой — никакого кода инициализации пакету не требуется.
