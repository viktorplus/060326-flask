# Настройки пакета: всё, что может меняться от окружения к окружению, — здесь.

# Путь к файлу-«базе данных» со списком сотрудников.
# Путь ОТНОСИТЕЛЬНЫЙ и разрешается от текущего рабочего каталога процесса,
# а не от расположения этого файла. Запускать проект нужно из его каталога
# (cd l03_rest_api && python app.py) — тогда файл окажется рядом с кодом.
# Запуск из другого каталога ошибки не даст: приложение молча создаст пустой
# employees_data.json там, откуда его запустили.
DATA_FILE = "employees_data.json"
