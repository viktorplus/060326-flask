# Файл-маркер: его наличие превращает каталог в Python-пакет,
# то есть разрешает импорты вида `from l08_orm_loading.app import User`.
# Запускать app.py всё равно нужно из каталога проекта: db.sqlite ищется
# относительно текущего рабочего каталога, а не расположения файла.
# Файл намеренно пустой — никакого кода инициализации пакету не требуется.
