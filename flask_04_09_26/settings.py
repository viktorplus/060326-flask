# Настройки пакета: всё, что может меняться от окружения к окружению, — здесь.

# Путь к файлу-«базе данных» со списком сотрудников.
# Путь ОТНОСИТЕЛЬНЫЙ, поэтому он разрешается от текущего рабочего каталога процесса,
# а не от расположения этого файла: приложение нужно запускать из корня проекта
# (python -m flask_04_09_26.app), иначе файл будет создан не там, где ожидается.
DATA_FILE = "flask_04_09_26/employees_data.json"
