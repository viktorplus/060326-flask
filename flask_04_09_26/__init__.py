# Файл-маркер: его наличие превращает каталог в Python-пакет.
# Именно благодаря ему работают относительные импорты внутри app.py, schemas.py и utils.py
# (`from .schemas import Employee`), а приложение запускается как `python -m flask_04_09_26.app`.
# Файл намеренно пустой — никакого кода инициализации пакету не требуется.
